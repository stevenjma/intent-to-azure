export default function Home() {
  return (
    <main>
      <h1>next-openai</h1>
      <p>
        A Next.js app with a chat route that calls the OpenAI SDK. azx should detect
        <code>web-compute</code> and <code>chat-model</code>, extract the
        <code>gpt-4o</code> model id, and plan an OpenAI account plus a model
        deployment.
      </p>
    </main>
  );
}
