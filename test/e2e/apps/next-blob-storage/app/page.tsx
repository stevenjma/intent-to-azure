export default function Home() {
  return (
    <main>
      <h1>next-blob-storage</h1>
      <p>
        A Next.js app that reads and writes Azure Blob Storage. azx should detect
        <code>web-compute</code> and <code>object-storage</code> and plan a storage
        account plus a blob container.
      </p>
    </main>
  );
}
