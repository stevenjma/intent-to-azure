export default function Home() {
  return (
    <main>
      <h1>next-prisma-postgres</h1>
      <p>
        A Next.js app with a Prisma schema targeting PostgreSQL. azx should detect
        <code>web-compute</code> and <code>transactional-relational</code> and plan a
        Container App plus a PostgreSQL flexible server and database.
      </p>
    </main>
  );
}
