# Copy to .env.local and fill in. Placeholders only — no secrets are committed.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/contoso
OPENAI_API_KEY=
AZURE_STORAGE_CONNECTION_STRING=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
